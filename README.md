# configurador-protos
configurador de cascos protos, primera prueba

## Levantar servidor local con Python

Desde la carpeta raíz del proyecto:

```bash
python3 -m http.server 8000
```

Luego abre en el navegador:

```text
http://localhost:8000/index.html
```
